framework:
    messenger:
        failure_transport: failed

        transports:
            async:
                dsn: '%env(DATABASE_URL)%'
                options:
                    use_notify: false
            failed:
                dsn: '%env(DATABASE_URL)%'
                options:
                    queue_name: failed

        routing:
            'App\Messenger\Message\NotificationMessage': async

when@test:
    framework:
        messenger:
            transports:
                async: 'in-memory://'
