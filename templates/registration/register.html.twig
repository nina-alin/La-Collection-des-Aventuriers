{% extends 'base.html.twig' %}

{% block title %}Inscription — La Collection{% endblock %}

{% block body %}
<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-12 col-md-8 col-lg-6">
            <h1 class="mb-4">Créer un compte</h1>

            {% for message in app.flashes('error') %}
                <div class="alert alert-danger" role="alert">{{ message }}</div>
            {% endfor %}

            {{ form_start(registrationForm, {attr: {novalidate: 'novalidate'}}) }}

            <div class="form-group">
                {{ form_label(registrationForm.pseudo, 'Pseudo', {label_attr: {class: 'form-label'}}) }}
                {{ form_widget(registrationForm.pseudo, {attr: {class: 'input' ~ (registrationForm.pseudo.vars.errors|length ? ' is-invalid' : '')}}) }}
                {% if registrationForm.pseudo.vars.errors|length %}
                    {% for error in registrationForm.pseudo.vars.errors %}
                        <p class="form-help error">{{ error.message }}</p>
                    {% endfor %}
                {% endif %}
            </div>

            <div class="form-group">
                {{ form_label(registrationForm.email, 'Adresse email', {label_attr: {class: 'form-label'}}) }}
                {{ form_widget(registrationForm.email, {attr: {class: 'input' ~ (registrationForm.email.vars.errors|length ? ' is-invalid' : '')}}) }}
                {% if registrationForm.email.vars.errors|length %}
                    {% for error in registrationForm.email.vars.errors %}
                        <p class="form-help error">{{ error.message }}</p>
                    {% endfor %}
                {% endif %}
            </div>

            <div class="form-group">
                {{ form_label(registrationForm.plainPassword.first, 'Mot de passe', {label_attr: {class: 'form-label'}}) }}
                {{ form_widget(registrationForm.plainPassword.first, {attr: {class: 'input' ~ (registrationForm.plainPassword.vars.errors|length ? ' is-invalid' : '')}}) }}
            </div>

            <div class="form-group">
                {{ form_label(registrationForm.plainPassword.second, 'Confirmer le mot de passe', {label_attr: {class: 'form-label'}}) }}
                {{ form_widget(registrationForm.plainPassword.second, {attr: {class: 'input' ~ (registrationForm.plainPassword.vars.errors|length ? ' is-invalid' : '')}}) }}
                {% if registrationForm.plainPassword.vars.errors|length %}
                    {% for error in registrationForm.plainPassword.vars.errors %}
                        <p class="form-help error">{{ error.message }}</p>
                    {% endfor %}
                {% endif %}
            </div>

            <div class="form-group">
                <label class="choice">
                    {{ form_widget(registrationForm.rgpdConsent) }}
                    <span>
                        J'accepte les
                        <a href="/politique-de-confidentialite" target="_blank">conditions d'utilisation et la politique de confidentialité</a>.
                    </span>
                </label>
                {% if registrationForm.rgpdConsent.vars.errors|length %}
                    {% for error in registrationForm.rgpdConsent.vars.errors %}
                        <p class="form-help error">{{ error.message }}</p>
                    {% endfor %}
                {% endif %}
            </div>

            <div class="d-flex flex-column gap-3 mt-4">
                <button type="submit" class="btn btn-primary btn-block btn-lg">
                    Créer mon compte
                </button>

                <div class="text-center" style="color: var(--ink-muted); font-size: var(--fs-sm);">ou</div>

                <a href="{{ path('app_oauth_google') }}"
                   class="btn btn-block"
                   data-loading-text="Redirection vers Google…"
                   onclick="this.classList.add('disabled'); this.setAttribute('aria-disabled','true'); this.innerHTML='<span class=\'spinner-border spinner-border-sm\' role=\'status\'></span> Redirection vers Google…';">
                    <svg width="18" height="18" viewBox="0 0 24 24" style="flex-shrink:0">
                        <path fill="#4285F4" d="M22.56 12.25c0-.78-.07-1.53-.2-2.25H12v4.26h5.92c-.26 1.37-1.04 2.53-2.21 3.31v2.77h3.57c2.08-1.92 3.28-4.74 3.28-8.09z"/>
                        <path fill="#34A853" d="M12 23c2.97 0 5.46-.98 7.28-2.66l-3.57-2.77c-.98.66-2.23 1.06-3.71 1.06-2.86 0-5.29-1.93-6.16-4.53H2.18v2.84C3.99 20.53 7.7 23 12 23z"/>
                        <path fill="#FBBC05" d="M5.84 14.09c-.22-.66-.35-1.36-.35-2.09s.13-1.43.35-2.09V7.07H2.18C1.43 8.55 1 10.22 1 12s.43 3.45 1.18 4.93l3.66-2.84z"/>
                        <path fill="#EA4335" d="M12 5.38c1.62 0 3.06.56 4.21 1.64l3.15-3.15C17.45 2.09 14.97 1 12 1 7.7 1 3.99 3.47 2.18 7.07l3.66 2.84c.87-2.6 3.3-4.53 6.16-4.53z"/>
                    </svg>
                    Se connecter avec Google
                </a>
            </div>

            {{ form_end(registrationForm) }}

            <p class="text-center mt-4" style="font-size: var(--fs-sm); color: var(--ink-muted);">
                Déjà un compte ?
                <a href="{{ path('app_login') }}">Se connecter</a>
            </p>
        </div>
    </div>
</div>
{% endblock %}
